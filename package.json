{
	"scripts": {
		"start": "npm-run-all --parallel server compile-watch",
		"test": "eslint --cache --ext .js --ext .jsx --ignore-path .gitignore *.js lib components && echo ✓",
		"dist": "npm run compile && npm run site",
		"site": "fractal build",
		"server": "fractal start --watch",
		"compile": "faucet --no-fingerprint --compact",
		"compile-watch": "npm run compile -- --watch"
	},
	"dependencies": {},
	"devDependencies": {
		"complate-fractal": "github:complate/complate-fractal#no-eval",
		"eslint-config-fnd-jsx": "^1.1.0",
		"eslint-plugin-compat": "^1.0.4",
		"eslint-plugin-jsx-a11y": "^6.0.2",
		"faucet-pipeline-js": "^0.15.2",
		"faucet-pipeline-sass": "^0.7.0",
		"npm-run-all": "^4.1.1"
	}
}
